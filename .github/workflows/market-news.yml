name: Web3 Market News

on:
  schedule:
    - cron: '0 */5 * * *'   # 每5小时整点：0:00 5:00 10:00 15:00 20:00 UTC
  workflow_dispatch:          # 支持手动触发

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Anthropic SDK
        run: npm install @anthropic-ai/sdk --no-save

      - name: Fetch market news
        run: node scripts/fetch_news.mjs market
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Apply news to news.json
        run: node apply_news.mjs news_input.json

      - name: Commit and push
        run: |
          git config user.name "web3-jobs-bot"
          git config user.email "gmjobs@users.noreply.github.com"
          git add news.json
          git diff --cached --quiet && echo "No changes to commit" || \
            git commit -m "chore: update market news [bot]"
          git push
